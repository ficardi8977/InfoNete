{{> header}}
<div class="container">
        <div class="row justify-content-center "> 
                <div class="text-center">
                    <h1>Cambiar rol de usuario </h1>
                </div>  
                <div class="alert alert-primary" role="alert">
                Seleccioné un nuevo rol.
                </div>
                <div class="text-center">
                        <form action="/usuario/modificarUsuario" method="POST">
                        <div class="input-group mb-3">
                        <div class="input-group-prepend">
                        <label class="input-group-text" for="inputGroupSelect01">Roles</label>
                        </div>
                        <select name="IdTipoUsuario" class="custom-select" id="inputGroupSelect01">
                        <option selected>Elegí un Rol...</option>
                        {{#tiposUsuarios}}
                        <option value={{Id}}>{{Nombre}}</option>
                        {{/tiposUsuarios}}
                        </select>
                        </div>

                                <input type="hidden" name="IdUsuario" value={{IdUsuario}} />
                                <button class="btn btn-success" type="submit">Modificar </button> 
                        </form>

                </div>

                </div>                   
        </div> 
</div>        
{{> footer}}