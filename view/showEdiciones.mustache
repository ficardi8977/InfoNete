{{> header}}

<div class="container ">
    <div class="d-flex justify-content-md-start pb-4">
        <a href="/ediciones/volver" class="btn btn-dark d-flex align-items-center"><i class="bi bi-arrow-left-circle fs-5 me-2"></i>Volver</a>
    </div>
    <div>
        <table class="table table-hover">
            <thead class="table-dark ">
            <tr class="text-center">
                <th scope="col">Número</th>
                <th scope="col">Fecha</th>
                <th scope="col">Precio</th>
                <th scope="col">Acciones</th>
            </tr>
            </thead>
            {{#ediciones}}
                <tbody>
                <tr class="text-center align-middle">
                    <td>{{Numero}}</td>
                    <td>{{Fecha}}</td>
                    <td>{{precio}}</td>
                    <td>
                        <div class="d-flex justify-content-center">
                            <!--<div class="px-2">
                                <form action="/ediciones/modificar" method="GET">
                                    <input type="hidden" name="Id" value={{Id}}>
                                    <button class="btn btn-primary" type="submit" >Modificar Edición</a>
                                </form>
                            </div>-->
                            <div class="px-2">
                                <form action="/ediciones/bajaEdicion" method="POST">
                                    <input type="hidden" name="Id" value={{Id}}>
                                    <input type="hidden" name="IdProducto" value={{IdProducto}}>
                                    <button class="btn btn-danger" type="submit" >Bajar Edición</a>
                                </form>
                            </div>
                        </div>
                    </td>
                </tr>
                </tbody>
            {{/ediciones}}
        </table>
    </div>

</div>

{{> footer}}